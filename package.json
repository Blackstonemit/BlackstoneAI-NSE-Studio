{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "format": "prettier --write \"artifacts/**/*.{ts,tsx,css,html}\" \"lib/**/*.{ts,tsx,css,html}\" \"scripts/**/*.{ts,tsx,css,html}\" \"*.{json,yaml,yml,md}\"",
    "format:check": "prettier --check \"artifacts/**/*.{ts,tsx,css,html}\" \"lib/**/*.{ts,tsx,css,html}\" \"scripts/**/*.{ts,tsx,css,html}\" \"*.{json,yaml,yml,md}\"",
    "check": "pnpm run typecheck && pnpm run format:check"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.9.6"
  }
}
